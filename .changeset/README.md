# Changesets

This folder holds pending release notes for `@heybray/scenarios-{server,client}`.
Merge a "Version Packages" PR to bump versions; merging that PR triggers npm publish
via `.github/workflows/release.yml` (requires `NPM_TOKEN`).
