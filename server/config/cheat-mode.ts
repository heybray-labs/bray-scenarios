/**
 * Re-export shim. Cheat-mode config moved into @heybray/scenarios-server during
 * the Phase 6A extraction; this keeps the historical `server/config` path
 * resolvable for the server test suite.
 */
export * from "@heybray/scenarios-server/config/cheat-mode";
