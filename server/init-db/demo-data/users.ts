/**
 * Re-export shim. Demo seed fixtures moved into @heybray/scenarios-server
 * during the Phase 6A extraction; this keeps the historical path resolvable
 * for the server test suite (e.g. the gamification golden test).
 */
export * from "@heybray/scenarios-server/seed/demo-data/users";
