/**
 * Re-export shim. Moved into @heybray/scenarios-server during the Phase 6A
 * extraction; this keeps the historical `shared/` path resolvable.
 */
export * from "@heybray/scenarios-server/schema/agent/roleplay-app-config";
